@model Nullable<DateTime>
    @using System.Globalization

@{
    DateTime dt = DateTime.Now;
    if (Model != null)
    {
        dt = (System.DateTime)Model;

    }   

    @Html.TextBox("", String.Format("{0:g}", dt.ToString()),
     new { @class = "datefield", type = "string" })
} 
